<!DOCTYPE html>
<html lang="en">
<head>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comprobante</title>
</head>
<body>
    <% if user_signed_in? %>
        <% if current_user.usuario? %>
        <h1>  Comprobante de alquiler </h1>
        <p>Nombre y apellido: <%= @historial.user.nombre %> <%= @historial.user.apellido %> </p>
        <p> Vehiculo: <%= @historial.auto.marca %> <%= @historial.auto.modelo %> <%= @historial.auto.patente %> </p>
        <p> Fecha de entrega: <%= @historial.fecha_inicio %></p>
        <p> Fecha de devolucion: <%= @historial.fecha_fin %></p>
        <p> Cantidad horas de alquiler: <%= @historial.cant_horas %></p>
        <p> Costo: <%= @historial.monto %></p>
        <p> Costo por extender: 0 </p>
        <p> Multas: 0 </p>
        <img src="Logo_3.jpg" alt="imágen"  width="150" height="150"/>

        <% end %>
        <div class="py-2 d-print-none">
        <%= link_to "Imprimir", "#", class: "btn btn-light", onclick: "window.print()" %>
    <% else %>
        <h1>AlquilApp </h1>
    <% end %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>

</body>
</html>
